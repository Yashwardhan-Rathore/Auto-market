import { SuperAdminAccount } from "./account";import { SuperAdminAdmins } from "./admins";import { SuperAdminAnalytics } from "./analytics";import { SuperAdminDashboard } from "./dashboard";import { SuperAdminUsage } from "./usage";import { DataView } from "@/components/modules/data-view";import type { ModuleKey } from "@/permissions/permission-matrix";
export function SuperAdminPage({section}:{section:string}){if(section==="dashboard")return <SuperAdminDashboard/>;if(section==="admins")return <SuperAdminAdmins/>;if(section==="users")return <SuperAdminAdmins kind="users"/>;if(section==="billing")return <SuperAdminUsage/>;if(section==="ai-credits")return <SuperAdminUsage creditsOnly/>;if(section==="analytics")return <SuperAdminAnalytics/>;if(section==="account")return <SuperAdminAccount/>;return <DataView module={section as ModuleKey}/>}
