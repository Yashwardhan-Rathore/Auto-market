"use client";
import { useQuery } from "@tanstack/react-query";import { motion } from "framer-motion";import { Activity, Mail, MessageSquare, Workflow } from "lucide-react";import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";import { parseApiError } from "@/services/api-client";import { superAdminService } from "@/services/super-admin.service";
export function SuperAdminAnalytics(){const q=useQuery({queryKey:["sa-analytics"],queryFn:superAdminService.analytics});if(q.isLoading)return <div className="h-96 animate-pulse rounded-2xl bg-white"/>;if(q.isError)return <div className="sa-card p-10 text-red-600">{parseApiError(q.error)}</div>;const d=q.data!;const chart=[{name:"Email",sent:d.email.sent,rate:d.email.open_rate},{name:"SMS",sent:d.sms.sent,rate:d.sms.delivery_rate},{name:"WhatsApp",sent:d.whatsapp.sent,rate:d.whatsapp.read_rate},{name:"Workflow",sent:d.workflow.execution_count,rate:d.workflow.success_rate}];const cards=[{label:"Emails sent",value:d.email.sent,sub:`${d.email.open_rate}% open rate`,icon:Mail},{label:"SMS sent",value:d.sms.sent,sub:`${d.sms.delivery_rate}% delivery`,icon:MessageSquare},{label:"WhatsApp sent",value:d.whatsapp.sent,sub:`${d.whatsapp.read_rate}% read rate`,icon:Activity},{label:"Workflow executions",value:d.workflow.execution_count,sub:`${d.workflow.success_rate}% success`,icon:Workflow}];return <div><h1 className="sa-title">ANALYTICS</h1><p className="sa-subtitle">Communication and workflow performance from recorded events</p><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map((c,i)=><motion.article initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{delay:i*.07}} className="sa-card p-5" key={c.label}><div className="metric-icon"><c.icon size={20}/></div><strong className="mt-4 block text-3xl text-slate-950">{c.value}</strong><p className="text-sm text-slate-500">{c.label}</p><span className="mt-3 inline-block rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">{c.sub}</span></motion.article>)}</div><section className="sa-card mt-6 p-6"><h2 className="font-bold">Channel activity</h2><div className="mt-5 h-[360px]"><ResponsiveContainer width="100%" height="100%"><BarChart data={chart}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="sent" fill="#1670f8" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></div></section></div>}
